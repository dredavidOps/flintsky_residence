apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: django-servicemonitor
  labels:
    release: prometheus-stack
spec:
  selector:
    matchLabels:
      app: django
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
